# Political Sphere Development Environment Variables
# 
# This file documents all environment variables used in the development container.
# Copy this file to `.env.local` and customize values as needed.
# 
# IMPORTANT: Never commit `.env.local` to version control!

# =============================================================================
# Application Configuration
# =============================================================================

# Node.js environment (development, production, test)
NODE_ENV=development

# Logging level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=debug

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL connection URL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://dev:dev_password@postgres:5432/political_sphere_dev

# Individual database components (alternative to DATABASE_URL)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=political_sphere_dev
DB_USER=dev
DB_PASSWORD=dev_password

# Database connection pool settings
DB_POOL_MIN=2
DB_POOL_MAX=10

# =============================================================================
# Redis Configuration
# =============================================================================

# Redis connection URL
REDIS_URL=redis://redis:6379

# Individual Redis components
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# =============================================================================
# Authentication & Security
# =============================================================================

# JWT secrets (MUST be at least 32 characters)
# IMPORTANT: Use secure random strings in production!
JWT_SECRET=dev_jwt_secret_min_32_chars_required_for_development_only
JWT_REFRESH_SECRET=dev_refresh_secret_min_32_chars_required_for_development

# JWT token expiry times
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Session configuration
SESSION_SECRET=dev_session_secret_min_32_chars_required_for_development

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:4000

# =============================================================================
# API Configuration
# =============================================================================

# API server port
API_PORT=4000

# API base URL
API_BASE_URL=http://localhost:4000

# API rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# =============================================================================
# Frontend Configuration
# =============================================================================

# Web app port
WEB_PORT=3000

# Public URL for the web app
PUBLIC_URL=http://localhost:3000

# =============================================================================
# Game Server Configuration
# =============================================================================

# Game server port (WebSocket)
GAME_SERVER_PORT=5000

# Game tick rate (updates per second)
GAME_TICK_RATE=30

# =============================================================================
# Worker Configuration
# =============================================================================

# Worker service port
WORKER_PORT=8080

# Background job concurrency
WORKER_CONCURRENCY=5

# =============================================================================
# Docker Configuration
# =============================================================================

# Docker host (for Docker-in-Docker)
DOCKER_HOST=unix:///var/run/docker.sock

# =============================================================================
# AWS Configuration (Optional)
# =============================================================================

# AWS region
# AWS_REGION=us-east-1

# AWS credentials (not recommended - use IAM roles instead)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# =============================================================================
# Monitoring & Observability
# =============================================================================

# Enable OpenTelemetry tracing
OTEL_ENABLED=false

# OpenTelemetry exporter endpoint
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Sentry DSN for error tracking (optional)
# SENTRY_DSN=

# =============================================================================
# Feature Flags
# =============================================================================

# Enable experimental features
ENABLE_EXPERIMENTAL_FEATURES=true

# Enable debug mode
DEBUG_MODE=true

# Enable performance profiling
ENABLE_PROFILING=false

# =============================================================================
# Email Configuration (Optional)
# =============================================================================

# SMTP configuration for email sending
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=noreply@political-sphere.com

# =============================================================================
# Third-Party Integrations (Optional)
# =============================================================================

# GitHub OAuth (for authentication)
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# Google OAuth (for authentication)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Stripe (for payments)
# STRIPE_PUBLISHABLE_KEY=
# STRIPE_SECRET_KEY=

# =============================================================================
# Development Tools
# =============================================================================

# Disable telemetry
NEXT_TELEMETRY_DISABLED=1
DO_NOT_TRACK=1

# NPM configuration
NPM_CONFIG_LOGLEVEL=warn
NPM_CONFIG_UPDATE_NOTIFIER=false

# Playwright/Puppeteer
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

# =============================================================================
# Testing Configuration
# =============================================================================

# Test environment
# NODE_ENV=test

# Test database
# TEST_DATABASE_URL=postgresql://dev:dev_password@postgres:5432/political_sphere_test

# =============================================================================
# Notes
# =============================================================================

# To use these variables:
# 1. Copy this file: cp .env.example .env.local
# 2. Customize values in .env.local
# 3. .env.local is git-ignored and won't be committed
# 
# Environment variable precedence:
# 1. System environment variables (highest priority)
# 2. .env.local (local development overrides)
# 3. .env (committed defaults - use carefully)
# 4. docker-compose.yml environment section
# 5. devcontainer.json containerEnv section (lowest priority)
