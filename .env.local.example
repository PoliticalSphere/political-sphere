# Local development overrides — safe to keep local. DO NOT commit secrets here.
# Copy this file to .env.local and customize for your local environment.
#
# FAST_AI=1 enables a quicker, lower-rigor AI mode for iterative development (dev only).
# Set to 0 or unset in CI, Safe, or Audit runs.
# FAST_AI=1
