# ⚠️  SECURITY WARNING ⚠️
# These are EXAMPLE VALUES for local development only!
# NEVER use these values in production environments!
# Generate strong, random passwords for all services in production.
# 
# To generate a secure password, use:
#   openssl rand -base64 32
# Or on macOS:
#   uuidgen | shasum -a 256 | cut -c1-32

# Shared environment variables consumed by scripts/docker-compose
COMPOSE_PROJECT_NAME=politicalsphere

# Database Configuration
# ⚠️ CHANGE THESE IN PRODUCTION! Use strong passwords (32+ characters)
POSTGRES_USER=political
POSTGRES_PASSWORD=CHANGE_ME_NEVER_USE_IN_PRODUCTION_$(openssl rand -base64 32)
POSTGRES_DB=political_dev

# Redis Configuration
# ⚠️ CHANGE THIS IN PRODUCTION!
REDIS_PASSWORD=CHANGE_ME_NEVER_USE_IN_PRODUCTION_$(openssl rand -base64 32)

# Admin Credentials
# ⚠️ CHANGE THESE IN PRODUCTION! Use strong passwords with special characters
AUTH_ADMIN_USER=admin
AUTH_ADMIN_PASSWORD=CHANGE_ME_NEVER_USE_IN_PRODUCTION_$(openssl rand -base64 32)

# AWS Configuration
AWS_REGION=us-east-1
LOCALSTACK_PROFILE=default

# Security Keys (Generate unique values for each environment)
# ⚠️ CRITICAL: Generate new values for production using:
#     node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_NEVER_USE_IN_PRODUCTION
CSRF_SECRET=CHANGE_ME_NEVER_USE_IN_PRODUCTION
ENCRYPTION_KEY=CHANGE_ME_NEVER_USE_IN_PRODUCTION

# API Configuration
API_BASE_URL=http://localhost:4000
FRONTEND_URL=http://localhost:3000

# SECURITY CHECKLIST FOR PRODUCTION:
# [ ] All passwords changed to strong, random values
# [ ] JWT_SECRET is a 64-byte random hex string
# [ ] CSRF_SECRET is a 64-byte random hex string
# [ ] ENCRYPTION_KEY is properly secured
# [ ] Database credentials stored in AWS Secrets Manager
# [ ] Redis password is strong and unique
# [ ] Admin password meets complexity requirements (16+ chars, mixed case, numbers, symbols)
# [ ] All secrets are never committed to git
# [ ] Environment-specific .env files are in .gitignore
