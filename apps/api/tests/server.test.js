/**
 * Deprecated duplicate test file. The ESM version `server.test.mjs` is authoritative.
 * This CJS placeholder is skipped to avoid ESM parse issues in certain runners.
 */
// eslint-disable-next-line no-undef
describe.skip('server.test.js (deprecated duplicate) — see server.test.mjs', () => {
  // eslint-disable-next-line no-undef
  test('noop placeholder', () => {
    // Intentionally empty
  });
});
