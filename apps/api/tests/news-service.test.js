/**
 * Deprecated duplicate test file. The ESM version `news-service.test.mjs` is authoritative.
 * This CJS placeholder is skipped to avoid ESM parse issues and duplicate execution.
 */
// eslint-disable-next-line no-undef
describe.skip('news-service.test.js (deprecated duplicate) — see news-service.test.mjs', () => {
  // eslint-disable-next-line no-undef
  test('noop placeholder', () => {
    // Intentionally empty
  });
});
